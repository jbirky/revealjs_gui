services:
  revealjs-editor:
    build: .
    ports:
      - "127.0.0.1:3002:3002"
    volumes:
      - revealjs-data:/app/server/data
      - revealjs-uploads:/app/server/uploads
    restart: unless-stopped

volumes:
  revealjs-data:
  revealjs-uploads:
